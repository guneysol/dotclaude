---
description: Commit changes, push to remote, and create a pull request
allowed-tools: Bash(git:*), Bash(gh:*)
disable-model-invocation: true
---

# Current State

## Branch
!`git branch --show-current`

## Status
!`git status --short`

## Staged Diff
!`git diff --cached --stat`

## Unstaged Diff
!`git diff --stat`

## Recent Commits (for context)
!`git log -3 --oneline`

---

# Instructions

Based on the git status above:

1. **Stage all changes** (unless I specify otherwise)
2. **Commit** with a conventional commit message:
   - Format: `type(scope): description`
   - Types: feat, fix, refactor, docs, test, chore
   - Keep it concise but descriptive
3. **Push** to the current branch (create upstream if needed)
4. **Create a PR** using `gh pr create`:
   - Title matching the commit message
   - Description: briefly state what changed and why — can be very short, just enough for a reviewer to understand the intent
   - Don't add "Generated by Claude Code" to the description

If there are no changes to commit, let me know.
